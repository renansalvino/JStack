//Importa todo tipo de módulo
const { printName, lastName } = require('./printName');

printName(`Renan ${lastName}`);

//Or

// const modulo =require('./printName');
// modulo.printName (`Renan${modulo.lastName} `);